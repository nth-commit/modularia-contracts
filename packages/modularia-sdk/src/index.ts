import { Provider } from '@ethersproject/abstract-provider'

export type Modularia = {}

/**
 * Initializes the Modularia SDK.
 *
 * @param _ethersProvider
 * @returns An instance of the Modularia SDK, authorized by the given provider.
 */
export function initModularia(_ethersProvider: Provider): Modularia {
  return {}
}
